name: toml-config
version: 0.4.0

dependencies:
  toml:
    # github: manastech/crystal-toml
    # version: 0.5.0
    # # until crystal-0.20 patch will be merged
    github: maiha/crystal-toml
    # version: ~> 0.6.0
    branch: crystal-0.25

authors:
  - maiha <maiha@wota.jp>

license: MIT
